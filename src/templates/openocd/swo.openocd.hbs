init

{{#unless (lookup ports 0)}}
itm port 0 off
{{/unless}}
{{#each ports}}
itm port {{this}} on
{{/each}}

tpiu config {{#if pipe}}internal {{pipe}}{{else}}external{{/if}} uart off {{config.probe.swo.reset-freq}} {{config.probe.swo.baud-rate}}

{{#if reset}}
reset run
{{/if}}
