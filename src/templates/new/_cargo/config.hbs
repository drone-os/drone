[build]
target = '{{device_target}}'
rustflags = [
    '--cfg', '{{platform_flag_name}}="{{platform_flag}}"',
    '--cfg', '{{bindings_flag_name}}="{{bindings_flag}}"',
]

[target.{{device_target}}]
rustflags = [
    '--cfg', '{{platform_flag_name}}="{{platform_flag}}"',
    '--cfg', '{{bindings_flag_name}}="{{bindings_flag}}"',
    '-C', 'linker=drone-ld',
]
