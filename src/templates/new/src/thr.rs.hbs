//! The threads.

pub use drone_{{platform_name}}::thr::{init, init_extended};
pub use drone_{{bindings_name}}_map::thr::*;

use drone_{{platform_name}}::thr;

thr::nvic! {
    /// Thread-safe storage.
    thread => pub Thr {};

    /// Thread-local storage.
    local => pub ThrLocal {};

    /// Vector table.
    vtable => pub Vtable;

    /// Thread token set.
    index => pub Thrs;

    /// Threads initialization token.
    init => pub ThrsInit;

    threads => {
        exceptions => {
            /// All classes of faults.
            pub hard_fault;
        };
    };
}
